
@{
    ViewData["Title"] = "Index";
    int count = 0;
}
@*@model List<AppUser>*@
<div>
    <row>
        <div class="col-md-10 mx-auto">
            <a type="button" class="btn btn-primary" asp-controller="CreateAdmin" asp-action="Create">Create</a>
        </div>
        <div class="col-md-10 mx-auto">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">UserName</th>
                        <th scope="col">FullName</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (AppUser usr in Model)
                    {
                        count++;
                        <tr>
                            <th scope="row">@usr.UserName</th>
                            <td>@usr.FullName</td>
                            <td>
                                <a asp-controller="Author" asp-action="Edit" asp-route-id="@usr.Id" type="button" class="btn btn-warning">Edit</a>
                                <a asp-controller="Author" asp-action="Delete" asp-route-id="@usr.Id" type="button" class="btn btn-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </row>
</div>

